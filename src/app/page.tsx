"Use Client";
import React from "react";
import { Button } from "@/components/ui/button";
import { Link } from "lucide-react";

const page = () => {
  return (
    <div>
      <Button>
        
      </Button>
    </div>
  );
};

export default page;
